"""
Service concerning the viewer pane.

The ViewerService class defines services related
to the viewer pane, which is meant for visualizing
renders and media within the app.
"""

from utils.singleton import Singleton


class ViewerService(metaclass=Singleton):
    """Service concerning the viewer pane."""